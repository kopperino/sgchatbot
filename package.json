{
  "name": "steinsgate-rag-chatbot",
  "version": "1.0.0",
  "description": "RAG chatbot for Steins;Gate anime using React, Express, and Langchain",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace=client && npm install --workspace=server",
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build:client": "npm run build --workspace=client",
    "scrape": "npm run scrape --workspace=server",
    "process": "npm run process --workspace=server",
    "embed": "npm run embed --workspace=server",
    "setup:data": "npm run scrape --workspace=server && npm run process --workspace=server && npm run embed --workspace=server"
  },
  "keywords": [
    "steinsgate",
    "rag",
    "chatbot",
    "langchain",
    "openai",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
